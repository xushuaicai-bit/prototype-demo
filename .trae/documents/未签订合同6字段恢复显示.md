# 未签订合同6个字段恢复显示计划

## Summary

将合同状态=未签订时仍需显示的6个字段（合同分类、工作范围及内容、主要工程量、初始策划金额、合同选用形式、计划签订日期）的置空条件移除，恢复为正常显示。

## 文件

`e:\trae_demo_env\营收看板\src\components\supply-chain\SupplierContractReport.vue`

## 改动内容

对以下6列的 `#default` template 移除 `signStatus === '未签订' ? '' :` 条件，直接显示原值：

| 列 | prop | 当前代码 | 改为 |
|----|------|---------|------|
| 合同分类 | category | `scope.row.signStatus === '未签订' ? '' : scope.row.category` | `scope.row.category` |
| 工作范围及内容 | workScope | `scope.row.signStatus === '未签订' ? '' : scope.row.workScope` | `scope.row.workScope` |
| 主要工程量 | workVolume | `scope.row.signStatus === '未签订' ? '' : scope.row.workVolume` | `scope.row.workVolume` |
| 初始策划金额 | plannedAmount | `scope.row.signStatus === '未签订' ? '' : formatNumber(scope.row.plannedAmount)` | `formatNumber(scope.row.plannedAmount)` |
| 合同选用形式 | contractForm | `scope.row.signStatus === '未签订' ? '' : scope.row.contractForm` | `scope.row.contractForm` |
| 计划签订日期 | plannedDate | `scope.row.signStatus === '未签订' ? '' : scope.row.plannedDate` | `scope.row.plannedDate` |

## 不变（仍置空）的6列

供应商名称、供应商等级、分包合同名称、实际签订日期、合同开始时间、合同计划/实际结束时间 — 这些保持置空不变。

## Verification

1. 未签订合同行的合同分类、工作范围及内容、主要工程量、初始策划金额、合同选用形式、计划签订日期正常显示
2. 未签订合同行的供应商名称、供应商等级、分包合同名称、实际签订日期、合同开始时间、合同计划/实际结束时间仍为空
3. GetDiagnostics 无错误
