# 科研费用管控模块图表 — 完成计划

## 一、任务概要

在 `TechResearchDashboard.vue` 的"科研费用管控"卡片中，用 ECharts 实现一种"柱状图 + 每根柱子顶部的水平计划指标线"的展示方式，替代普通柱状图展示计划值。

核心规则：
- 柱状高度 = 实际完成值（actual）
- 横线位置 = 计划目标值（plan）
- 每个基层单位下两根柱子：[专项] [配套]
- 单位：万元
- 点击柱子：仅弹出提示框（不跳转）

## 二、当前状态分析

文件：`e:\trae_demo_env\营收看板\src\views\TechResearchDashboard.vue`（共 644 行）

**已完成的部分：**
1. 模板（第 156–187 行）：科研费用管控卡片已改为整行满宽，含图例 4 项 + `<div ref="expenseChartRef" class="w-full h-[380px]"></div>`
2. 变量（第 229、235 行）：`expenseChartRef` / `expenseChart` 已声明
3. `handleResize`（第 615 行）：已含 `expenseChart?.resize()`
4. `onUnmounted`（第 641 行）：已含 `expenseChart?.dispose()`

**缺失的部分（本计划要补齐）：**
1. `onMounted`（第 626–634 行）未调用 `initExpenseChart()`
2. 缺少 `watch(selectedQuarter, () => updateExpenseChart())`
3. 缺少 4 个函数：`EXP_COLOR` / `buildExpenseData` / `initExpenseChart` / `updateExpenseChart`，需插入到 `updateAchievementChart` 结束（第 608 行）与 `const handleResize`（第 610 行）之间

**数据结构确认**（`mockData.js` 第 466–871 行）：
```js
researchDashboardData.expenses.byCompany = {
  Q1: [{ name:'管网事业部', special:{plan:60,actual:54}, supporting:{plan:40,actual:44} }, ...9 个单位],
  Q2: [...], Q3: [...],
  Q4: [{ name:'管网事业部', special:{plan:68,actual:0}, supporting:{plan:44,actual:0} }, ...]  // actual=0 展示未开工风险
}
```

## 三、具体修改

### 修改 1：插入 4 个图表函数

位置：`updateAchievementChart` 函数结束（第 608 行 `}`）之后、`const handleResize = () => {`（第 610 行）之前。

插入以下代码：

```js
const EXP_COLOR = {
  special: '#3b82f6',
  support: '#10b981',
  mutedSpecial: '#bfdbfe',
  mutedSupport: '#a7f3d0',
  planLine: '#f59e0b',
  risk: '#ef4444',
  ok: '#10b981'
}

const buildExpenseData = (quarter) => {
  const list = researchData.expenses.byCompany[quarter] || []
  const categories = list.map(item => item.name)
  return { categories, list }
}

const initExpenseChart = () => {
  if (!expenseChartRef.value) return
  expenseChart = echarts.init(expenseChartRef.value)
  updateExpenseChart()
  expenseChart.on('click', (params) => {
    if (params.componentSubType !== 'bar') return
    const d = buildExpenseData(selectedQuarter.value)
    const idx = d.categories.indexOf(params.name)
    if (idx < 0) return
    const type = params.seriesName === '专项' ? 'special' : 'supporting'
    const item = d.list[idx][type]
    const plan = item.plan
    const actual = item.actual
    const rate = plan ? Math.round(actual / plan * 100) : 0
    const dev = actual - plan
    const NL = '\n'
    window.alert(
      '基层单位：' + params.name + NL +
      '类型：' + params.seriesName + NL +
      '计划值：' + plan + '万' + NL +
      '实际值：' + actual + '万' + NL +
      '完成率：' + rate + '%' + NL +
      '偏差：' + dev + '万'
    )
  })
}

const updateExpenseChart = () => {
  if (!expenseChart) return
  const d = buildExpenseData(selectedQuarter.value)
  const list = d.list
  const categories = d.categories

  const specialData = list.map(item => {
    const plan = item.special.plan
    const actual = item.special.actual
    const rate = plan ? actual / plan : 0
    return {
      value: actual,
      itemStyle: { color: rate >= 1 ? EXP_COLOR.special : EXP_COLOR.mutedSpecial }
    }
  })

  const supportData = list.map(item => {
    const plan = item.supporting.plan
    const actual = item.supporting.actual
    const rate = plan ? actual / plan : 0
    return {
      value: actual,
      itemStyle: { color: rate >= 1 ? EXP_COLOR.support : EXP_COLOR.mutedSupport }
    }
  })

  // 计划线 custom 数据：[类别索引, 计划值, 侧别(-1=专项左柱, +1=配套右柱)]
  const customData = []
  list.forEach((item, i) => {
    customData.push([i, item.special.plan, -1])
    customData.push([i, item.supporting.plan, 1])
  })

  const labelFormatter = (params) => {
    const idx = params.dataIndex
    const type = params.seriesName === '专项' ? 'special' : 'supporting'
    const item = list[idx][type]
    const plan = item.plan
    const actual = item.actual
    const rate = plan ? Math.round(actual / plan * 100) : 0
    const dev = actual - plan
    const rateKey = rate >= 100 ? 'rateOk' : 'rateRisk'
    const devKey = dev < 0 ? 'devRisk' : 'devOk'
    const devSign = dev >= 0 ? '+' : ''
    return '{' + rateKey + '|' + rate + '%}\n{val|' + actual + '万}\n{' + devKey + '|' + devSign + dev + '万}'
  }

  const richConfig = {
    rateOk: { color: EXP_COLOR.ok, fontSize: 12, fontWeight: 'bold', lineHeight: 16 },
    rateRisk: { color: EXP_COLOR.risk, fontSize: 12, fontWeight: 'bold', lineHeight: 16 },
    val: { color: '#374151', fontSize: 11, lineHeight: 14 },
    devOk: { color: EXP_COLOR.ok, fontSize: 10, lineHeight: 12 },
    devRisk: { color: EXP_COLOR.risk, fontSize: 10, lineHeight: 12 }
  }

  const tooltipFormatter = (params) => {
    const idx = params.dataIndex
    const type = params.seriesName === '专项' ? 'special' : 'supporting'
    const item = list[idx][type]
    const plan = item.plan
    const actual = item.actual
    const rate = plan ? Math.round(actual / plan * 100) : 0
    const dev = actual - plan
    return '<b>' + params.name + ' - ' + params.seriesName + '</b><br/>' +
      '计划值：' + plan + '万<br/>' +
      '实际值：' + actual + '万<br/>' +
      '完成率：' + rate + '%<br/>' +
      '偏差：' + dev + '万'
  }

  const option = {
    tooltip: { trigger: 'item', formatter: tooltipFormatter },
    grid: { left: '4%', right: '4%', bottom: '8%', top: '18%', containLabel: true },
    xAxis: {
      type: 'category',
      data: categories,
      axisLabel: { interval: 0, rotate: 25, fontSize: 10 },
      axisTick: { show: false },
      axisLine: { lineStyle: { color: '#e5e7eb' } }
    },
    yAxis: {
      type: 'value',
      name: '万元',
      nameTextStyle: { fontSize: 10, color: '#9ca3af' },
      axisLabel: { fontSize: 10, color: '#9ca3af' },
      splitLine: { lineStyle: { color: '#f3f4f6', type: 'dashed' } },
      axisLine: { show: false },
      axisTick: { show: false }
    },
    series: [
      {
        name: '专项',
        type: 'bar',
        data: specialData,
        barWidth: 20,
        barGap: '30%',
        label: { show: true, position: 'top', formatter: labelFormatter, rich: richConfig }
      },
      {
        name: '配套',
        type: 'bar',
        data: supportData,
        barWidth: 20,
        label: { show: true, position: 'top', formatter: labelFormatter, rich: richConfig }
      },
      {
        name: '计划线',
        type: 'custom',
        data: customData,
        z: 10,
        renderItem: (params, api) => {
          const catIdx = api.value(0)
          const planVal = api.value(1)
          const side = api.value(2)
          const point = api.coord([catIdx, planVal])
          const halfWidth = 10
          const offset = 13 * side
          return {
            type: 'line',
            shape: {
              x1: point[0] - halfWidth + offset,
              y1: point[1],
              x2: point[0] + halfWidth + offset,
              y2: point[1]
            },
            style: { stroke: EXP_COLOR.planLine, lineWidth: 2 }
          }
        }
      }
    ]
  }

  expenseChart.setOption(option, true)
}
```

### 修改 2：onMounted 中追加 initExpenseChart 调用

在 `onMounted` 的 `setTimeout` 回调中，`initAchievementChart()` 之后追加一行 `initExpenseChart()`。

```js
// 修改后：
onMounted(() => {
  setTimeout(() => {
    initTechChart()
    initKnowledgeChart()
    initProjectChart()
    initAchievementChart()
    initExpenseChart()          // ← 新增
    window.addEventListener('resize', handleResize)
  }, 100)
})
```

### 修改 3：追加 watch(selectedQuarter)

在现有 `watch(achievementTab, ...)` 之后追加：

```js
watch(selectedQuarter, () => updateExpenseChart())
```

## 四、设计要点说明

| 需求 | 实现方式 |
|------|----------|
| 两根柱子 [专项][配套] | 两个 bar series，barWidth=20，barGap='30%' |
| 柱状高度=实际完成值 | bar series data = actual 值 |
| 每柱水平计划指标线 | custom series renderItem 绘制线段，side=-1/+1 区分左右柱偏移 |
| 实际值标签 | label formatter rich text `{val|450万}` |
| 完成率标签 | `{rateOk/rateRisk|90%}`，>=100% 绿色 / <100% 红色 |
| 偏差标签 | `{devOk/devRisk|+/-50万}`，负值红色 / 正值绿色 |
| 达标柱体突出 | itemStyle.color 达标用主色，未达标用弱化色 |
| 计划线高亮色 | #f59e0b (橙) |
| Hover 浮层 | tooltip formatter 显示基层单位/计划/实际/完成率/偏差 |
| 点击仅提示 | window.alert 弹出详情 |
| 减少网格线 | splitLine dashed 浅灰，axisLine 隐藏 |
| 保留留白 | grid top:18% bottom:8% |

## 五、假设与决策

1. **点击行为**：用户已确认"暂不跳转仅提示"，使用 `window.alert`
2. **布局**：用户已确认"整行满宽展示"，模板已改好
3. **custom series 偏移计算**：barWidth=20、barGap='30%'(=6px) → 两柱中心间距 26px；专项偏移 offset=-13，配套偏移 offset=+13；计划线半宽 halfWidth=10（等于柱宽的一半，正好覆盖柱体宽度）
4. **`currentExpense` computed（第 264–266 行）**：当前未被模板使用，属于历史遗留，不删除以最小化改动
5. **Q4 actual=0 的展示**：完成率 0%，柱体高度为 0（几乎不可见），但计划线正常显示，标签显示"0%"、"-68万"等偏差，符合风险提示语义

## 六、验证步骤

1. 启动 dev server：`cd e:\trae_demo_env\营收看板 && npm run dev`
2. 浏览器打开科研技术看板页面
3. 验证项：
   - 科研费用管控卡片整行满宽，图例 4 项正确
   - 每个基层单位下两根柱子（专项蓝/配套绿），未达标柱体弱化色
   - 每根柱子顶部有橙色水平计划线
   - 柱子上方依次显示：完成率%、实际值万、偏差万
   - 切换 Q1–Q4，图表数据更新；Q4 柱体高度为 0 但计划线与标签正常
   - Hover 柱子显示浮层（5 项信息）
   - 点击柱子弹出 alert 提示
