# 营收汇总表 - 施工类总营收年月筛选计划

## 目标
为 RevenueSummary.vue 的「施工类总营收」tab 增加年月筛选功能：切换不同年月时，施工类表数据随之变化。仅施工类数据响应年月，产品类/其他类保持不变。

## 当前状态（基于 Phase 1 实际读取确认）

文件：`e:\trae_demo_env\营收看板\src\components\revenue\RevenueSummary.vue`

已完成的前置改动：
- L52：年月选择器已去掉 `v-show`，对施工类 tab 可见 ✅
- L672：`import { ref, computed, onMounted, watch } from 'vue'` ✅（已包含 watch）

待完成项（本次计划范围）：
- L685：`const summaryMonth = ref('')` —— 默认空，需改为 `ref(6)`
- L1010 之后：rawData 定义结束处，需新增 `generateConstructionData` 函数 + `constructionData` computed
- L1235：`calculateDerivedFields(rawData)` 需改为 `constructionData.value`
- L1244：同上
- 缺少 `watch([summaryYear, summaryMonth])` 重置分页逻辑
- L1333-1335 `handleReset` 仅清空 `selectedUnits`，未同步重置年月和分页

## 数据生成设计（确定性规则）

要求：**同年月稳定（同输入同输出）、不同年月数据不同**，并符合业务语义。

字段分类与变换策略：
- **月度类字段**（随月波动，乘 monthWave）：`monthlyPlanConstruction`、`monthlyPlanCompleted`、`monthlyPlanNew`、`monthlyActualConstruction`、`monthlyActualCompleted`、`monthlyActualNew`、`nextMonthPlan`
- **累计类字段**（随月递增，乘 cumFactor = m/6）：`actualConstruction`、`actualCompleted`、`actualNew`、`reportedRevenue`、`newContractAmount`
- **剩余合同存量**（随月递减，乘 declineFactor = 1.3 - m*0.04）：`remainingContract`
- **年度基准字段**（同年稳定，仅乘 yearBase）：`revenueTarget`、`carryForwardConstruction`、`completedPendingSettlement`、`planConstruction`、`planCompleted`、`planNew`

年份基准系数 yearBaseMap：`{ 2024: 0.85, 2025: 0.92, 2026: 1.0 }`（默认 1.0）
月份波动系数：`0.8 + Math.abs(Math.sin(y * 12 + m)) * 0.4`

## 实施步骤

### 步骤 1：默认月份改为 6
**文件**：`RevenueSummary.vue` L685

```js
// 原：
const summaryMonth = ref('')
// 改为：
const summaryMonth = ref(6)
```

### 步骤 2：新增 generateConstructionData + constructionData computed
**文件**：`RevenueSummary.vue`，插入位置：rawData 定义之后（L1010 `]` 之后、L1012 `const allUnits` 之前）

```js
// 按年月生成施工类数据（同年月稳定，不同年月数据不同）
const yearBaseMap = { 2024: 0.85, 2025: 0.92, 2026: 1.0 }
const generateConstructionData = (year, month) => {
  const y = parseInt(year) || 2026
  const m = parseInt(month) || 6
  const yearBase = yearBaseMap[y] ?? 1.0
  const cumFactor = m / 6
  const monthWave = 0.8 + Math.abs(Math.sin(y * 12 + m)) * 0.4
  const declineFactor = 1.3 - m * 0.04
  return rawData.map(item => {
    const r = { ...item }
    ;['monthlyPlanConstruction','monthlyPlanCompleted','monthlyPlanNew',
      'monthlyActualConstruction','monthlyActualCompleted','monthlyActualNew','nextMonthPlan']
      .forEach(f => { r[f] = Math.round(item[f] * yearBase * monthWave) })
    ;['actualConstruction','actualCompleted','actualNew','reportedRevenue','newContractAmount']
      .forEach(f => { r[f] = Math.round(item[f] * yearBase * cumFactor) })
    r.remainingContract = Math.round(item.remainingContract * yearBase * declineFactor)
    ;['revenueTarget','carryForwardConstruction','completedPendingSettlement',
      'planConstruction','planCompleted','planNew']
      .forEach(f => { r[f] = Math.round(item[f] * yearBase) })
    return r
  })
}
const constructionData = computed(() => generateConstructionData(summaryYear.value, summaryMonth.value))
```

### 步骤 3：filteredConstructionData 改用 constructionData
**文件**：`RevenueSummary.vue` L1235

```js
// 原：
const calculated = calculateDerivedFields(rawData)
// 改为：
const calculated = calculateDerivedFields(constructionData.value)
```

### 步骤 4：paginatedConstructionData 改用 constructionData
**文件**：`RevenueSummary.vue` L1244

```js
// 原：
const calculated = calculateDerivedFields(rawData)
// 改为：
const calculated = calculateDerivedFields(constructionData.value)
```

### 步骤 5：新增 watch 重置分页
**文件**：`RevenueSummary.vue`，插入位置：handleReset 函数之前（L1329 `const handleSearch` 之前）

```js
// 年月变化时重置施工类分页
watch([summaryYear, summaryMonth], () => {
  constructionPage.value = 1
})
```

### 步骤 6：handleReset 同步重置年月和分页
**文件**：`RevenueSummary.vue` L1333-1335

```js
// 原：
const handleReset = () => {
  selectedUnits.value = []
}
// 改为：
const handleReset = () => {
  selectedUnits.value = []
  summaryYear.value = '2026'
  summaryMonth.value = 6
  constructionPage.value = 1
}
```

## 不变更项（保持不动）
- 列定义 / visibleColumns / allColumnOptions
- 排序逻辑（sortable="custom" + handleSortChange + sortData + 三个 sorted*Data）
- 列设置弹窗 / 分组联动
- 合计行结构（calculateTotalsForFiltered 不变，自动随输入数据变化）
- 导出 Excel 逻辑
- 产品类 / 其他类表（productData、otherData 原始数组与排序不变）
- rawData 原始静态数据本身（作为基准，不修改）

## 验证步骤
1. dev 服务器（command_id: `9e2fac35-a449-4046-946a-197cb6c03612`，http://localhost:5173/）HMR 无报错
2. 进入「营收汇总表 → 施工类总营收」tab
3. 默认显示 2026 年 6 月数据
4. 切换月份（如 1 月 vs 12 月）：累计类字段（actualConstruction 等）随月递增；月度类字段随月波动；剩余合同存量随月递减；同年同月数据稳定
5. 切换年份（2024/2025/2026）：整体数据按 yearBase 缩放
6. 切换年月后分页回到第 1 页
7. 点击「重置」按钮：年月回到 2026/6、基层单位清空、分页回到第 1 页
8. 产品类 / 其他类 tab 不受年月切换影响
