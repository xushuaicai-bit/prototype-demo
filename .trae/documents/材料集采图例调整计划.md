# 材料集采合同情况 — 删除右上角图例，启用ECharts底部图例

## 修改
**文件**: [MaterialProcurementChart.vue](src/components/dashboard/MaterialProcurementChart.vue)

### 1. 删除右上角自定义图例（第56-63行）
```html
<!-- 删除 -->
<div class="flex mb-2 justify-end">
  <div class="flex flex-wrap gap-2">
    <div class="flex items-center text-xs" v-for="series in displaySeries" :key="series.name">
      <span class="w-3 h-3 rounded mr-1" :style="{ backgroundColor: series.color }"></span>
      <span class="text-gray-600">{{ series.name }}</span>
    </div>
  </div>
</div>
```

### 2. 启用 ECharts 内置 legend（第155-157行）
```js
// 改前
legend: { show: false }
// 改后
legend: {
  show: true,
  bottom: 0,
  itemWidth: 14,
  itemHeight: 10,
  textStyle: { fontSize: 11 }
}
```

### 3. grid.bottom 调整（第161行）
`bottom: '3%'` → `bottom: '15%'` 给底部图例留空间
