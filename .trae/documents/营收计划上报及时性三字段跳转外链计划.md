# 营收计划上报及时性三字段跳转外链计划

## 目标
产运看板（UnifiedDashboard.vue）"营收管理看板"分组的"营收计划上报及时性"模块中，"应上报"、"已上报"、"待上报"三个字段点击后在新标签页打开外链：
`https://www.smart-worksite.com/hj/#/micro/market/biz/ent/market/general/contract`

## 现状分析（Phase 1 实际读取确认）

文件：`e:\trae_demo_env\营收看板\src\views\UnifiedDashboard.vue`

### 三个字段当前状态（L244-272）
| 字段 | 位置 | 当前 @click | cursor-pointer |
|---|---|---|---|
| 应上报（圆环） | L247-261 div | 无 | 已有（hover:opacity-90） |
| 已上报 | L264-267 div | 无 | 无 |
| 待上报 | L268-271 div | 无 | 无 |

### 现有跳转函数模式（L1174-1196）
文件已有多个同类跳转函数，命名风格 `handleXxxClick`，用 `window.open(链接, '_blank')`：
```js
const handleProjectTotalClick = () => {
  window.open('https://www.smart-worksite.com/hj/#/micro/market/biz/ent/market/prod-project/index', '_blank')
}
```

## 改造方案

### 1. 新增跳转函数（L1176 handleProjectTotalClick 之后）
```js
const handleTimelinessReportClick = () => {
  window.open('https://www.smart-worksite.com/hj/#/micro/market/biz/ent/market/general/contract', '_blank')
}
```
遵循现有 handleXxxClick 命名风格，新标签页打开。

### 2. 模板改动（3 处）

**应上报（圆环）L247**：div 加 `@click="handleTimelinessReportClick"`（已有 cursor-pointer）
- 原：`<div :title="'完成率：' + timelinessSingle.percentage + '%'" class="relative w-24 h-24 cursor-pointer hover:opacity-90 flex-shrink-0">`
- 改：同上加 `@click="handleTimelinessReportClick"`

**已上报 L264**：
- 原：`<div class="text-gray-700">`
- 改：`<div class="text-gray-700 cursor-pointer hover:opacity-80 transition-opacity" @click="handleTimelinessReportClick">`

**待上报 L268**：
- 原：`<div class="text-gray-500">`
- 改：`<div class="text-gray-500 cursor-pointer hover:opacity-80 transition-opacity" @click="handleTimelinessReportClick">`

## 不变更项
- 橙色预警、黄色预警（本次不涉及）
- 圆环、数值显示、预警圆点等其余样式
- timelinessSingle 数据逻辑
- RevenueDashboard.vue（独立营收管理看板页，本次不改；如需同步可后续告知）
- 下半部分"X月营收偏差情况"模块

## 验证步骤
1. dev 服务器 HMR 无报错
2. 产运看板 → 营收管理看板 → 营收计划上报及时性：
   - 点击应上报（圆环）→ 新标签页打开外链
   - 点击已上报 → 新标签页打开外链
   - 点击待上报 → 新标签页打开外链
3. 三处 hover 有指针/透明度反馈
