# 删除供应商统计/Top10右上角重复图例

## 概述
删除 SupplierStatChart.vue 模板中标题右侧的自定义图例，避免与 ECharts 底部内置 legend 重复。

## 修改

**文件**: [SupplierStatChart.vue](src/components/dashboard/SupplierStatChart.vue) 第8-13行

删除模板中的自定义图例块：

```html
<!-- 删除以下代码 -->
<div class="flex flex-wrap gap-2">
  <div class="flex items-center text-xs" v-for="series in displaySeries" :key="series.name">
    <span class="w-3 h-3 rounded mr-1" :style="{ backgroundColor: series.color }"></span>
    <span class="text-gray-600">{{ series.name }}</span>
  </div>
</div>
```

ECharts 的 `legend: { show: true, bottom: 0 }` 已在底部显示图例，无需重复。
