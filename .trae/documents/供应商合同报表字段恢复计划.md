# 供应商合同签订报表字段恢复计划

## Summary

恢复旧版中被删除的4个表格列（供应商名称、供应商等级、合同状态、实际签订日期），并将"预警等级"列改回"签订状态"（显示已签订/未签订），同时保留当前新增的2个时间字段（合同开始时间、合同计划/实际结束时间）。

## Current State Analysis

### 当前表格列（16列，L157-L282）

| # | 列名 | prop | 状态 |
|---|------|------|------|
| 1 | 序号 | index | - |
| 2 | **预警等级** | signStatus | ⚠️ 需改回"签订状态" |
| 3 | 项目编号 | projectCode | - |
| 4 | 项目名称 | projectName | - |
| 5 | 基层单位（简称） | unit | - |
| 6 | 项目状态 | status | - |
| 7 | 合同分类 | category | - |
| 8 | 分包合同名称 | contractName | - |
| 9 | 工作范围及内容 | workScope | - |
| 10 | 主要工程量 | workVolume | - |
| 11 | 初始策划金额 | plannedAmount | - |
| 12 | 合同选用形式 | contractForm | - |
| 13 | 计划签订日期 | plannedDate | - |
| 14 | 合同开始时间 | contractStartDate | ✅ 保留 |
| 15 | 合同计划/实际结束时间 | contractEndDate | ✅ 保留 |
| 16 | 超期天数 | overdueDays | - |

### rawData 中已存在但未显示的字段

| 字段 | prop | 示例值 | 说明 |
|------|------|--------|------|
| 供应商名称 | `supplierName` | '北京华建工程有限公司' | ✅ 数据已完整 |
| 供应商等级 | `supplierLevel` | 'A' / 'B' / 'C' | ✅ 数据已完整 |
| 合同状态 | `contractStatus` | '正常履约' / '履约结束' | ✅ 数据已完整 |
| 实际签订日期 | `actualDate` | '2026-02-20' / '' | ✅ 数据已完整 |
| 签订状态 | `signStatus` | '已签订' / '未签订' | ✅ 数据完整，但当前列显示为预警等级 |

### 当前筛选条件（L94-L126）

- 项目名称 ✅
- 基层单位 ✅
- 项目状态 ✅
- 合同分类 ✅
- 预警等级（红/橙/黄/未签订）⚠️ 需改为签订状态筛选
- 超期天数 ✅

### 缺失的筛选条件（需恢复）

- ~~供应商名称~~ → 恢复
- ~~合同状态~~ → 恢复

## Proposed Changes

### 文件：`e:\trae_demo_env\营收看板\src\components\supply-chain\SupplierContractReport.vue`

#### 改动1：表格列 — 在"合同分类"后插入4列 + 修改"预警等级"为"签订状态"

**位置**: L206（合同分类列之后）+ L164（预警等级列）

在"合同分类"(category)列之后、"分包合同名称"(contractName)列之前插入：

```html
<!-- 新增：供应商名称 -->
<el-table-column
  label="供应商名称"
  prop="supplierName"
  width="160"
/>

<!-- 新增：供应商等级 -->
<el-table-column
  label="供应商等级"
  prop="supplierLevel"
  width="110"
  align="center"
>
  <template #default="scope">
    <span :class="getSupplierLevelClass(scope.row.supplierLevel)">
      {{ scope.row.supplierLevel }}
    </span>
  </template>
</el-table-column>

<!-- 新增：合同状态 -->
<el-table-column
  label="合同状态"
  prop="contractStatus"
  width="120"
  align="center"
>
  <template #default="scope">
    <span :class="getContractStatusClass(scope.row.contractStatus)">
      {{ scope.row.contractStatus }}
    </span>
  </template>
</el-table-column>
```

将 L165 的 `label="预警等级"` 改为 `label="签订状态"`，内容从预警标签改为原始 signStatus 值（已签订/未签订）。

#### 改动2：在"计划签订日期"后插入"实际签订日期"列

```html
<!-- 新增：实际签订日期 -->
<el-table-column
  label="实际签订日期"
  prop="actualDate"
  width="130"
  align="center"
/>
```

#### 改动3：恢复样式函数

添加两个缺失的样式函数：

```javascript
// 供应商等级样式
const getSupplierLevelClass = (level) => {
  const classMap = {
    'A': 'px-2 py-0.5 rounded text-xs font-medium bg-green-100 text-green-700',
    'B': 'px-2 py-0.5 rounded text-xs font-medium bg-blue-100 text-blue-700',
    'C': 'px-2 py-0.5 rounded text-xs font-medium bg-gray-100 text-gray-700'
  }
  return classMap[level] || ''
}

// 合同状态样式
const getContractStatusClass = (status) => {
  const classMap = {
    '正常履约': 'text-green-600 font-medium',
    '履约结束': 'text-gray-500',
    '违约中': 'text-red-500 font-medium'
  }
  return classMap[status] || ''
}
```

#### 改动4：修改签订状态列的显示逻辑

当前 `getWarningLabel()` 显示预警等级 → 改回直接显示 `signStatus`（已签订/未签订）：

```html
<template #default="scope">
  <span :class="scope.row.signStatus === '已签订' ? 'text-green-600 font-medium' : 'text-orange-500 font-medium'">
    {{ scope.row.signStatus }}
  </span>
</template>
```

#### 改动5：筛选条件调整

- L95: `"预警等级"` → `"签订状态"`
- L339: `allSignStatus` 从 `['红色预警','橙色预警','黄色预警','未签订']` 改回 `['已签订','未签订']`
- 筛选匹配逻辑：用 `signStatus` 直接匹配而非 `getWarningLabel()`

#### 改动6：恢复供应商名称和合同状态筛选器

在"合同分类"筛选器之后、"签订状态"筛选器之前插入：

```html
<div class="flex items-center">
  <label class="text-sm text-gray-600 mr-2">供应商名称：</label>
  <el-select v-model="selectedSupplier" placeholder="请选择供应商" class="w-64" clearable>
    <el-option v-for="s in allSuppliers" :key="s" :label="s" :value="s" />
  </el-select>
</div>

<div class="flex items-center">
  <label class="text-sm text-gray-600 mr-2">合同状态：</label>
  <el-select v-model="selectedContractStatus" placeholder="请选择合同状态" class="w-64" clearable>
    <el-option v-for="cs in allContractStatus" :key="cs" :label="cs" :value="cs" />
  </elselect>
</div>
```

对应的数据和变量：
- `selectedSupplier = ref('')`
- `selectedContractStatus = ref('')`
- `allSuppliers`: computed 从 rawData 提取去重
- `allContractStatus = ['正常履约', '履约结束', '违约中']`

#### 改动7：更新 computed 过滤逻辑

在 `tableData` computed 中增加对 `selectedSupplier` 和 `selectedContractStatus` 的过滤条件。

## 最终表格列顺序（20列）

```
序号 | 签订状态 | 项目编号 | 项目名称 | 基层单位 | 项目状态 | 合同分类 | 供应商名称 | 供应商等级 | 合同状态 | 分包合同名称 | 工作范围及内容 | 主要工程量 | 初始策划金额 | 合同选用形式 | 计划签订日期 | 实际签订日期 | 合同开始时间 | 合同计划/实际结束时间 | 超期天数
```

## Verification Steps

1. 表格应显示20列（原16列 + 供应商名称 + 供应商等级 + 合同状态 + 实际签订日期）
2. "签订状态"列显示"已签订"/"未签订"（非预警等级）
3. 供应商等级 A/B/C 有不同颜色标签
4. 合同状态有颜色区分
5. 筛选器包含：项目名称、基层单位、项目状态、合同分类、**供应商名称**、**合同状态**、签订状态、超期天数
6. 合同开始时间和合同结束时间字段保持不变
7. 分页功能正常
8. 无语法错误
