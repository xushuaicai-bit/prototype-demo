# 科研费用管控图表 — 移除柱上标签，改为悬浮显示

## 一、任务概要

科研费用管控图表中柱子上方的完成率、实际值、偏差标签过于拥挤。需要：
1. 移除柱子上的所有数字标签
2. 数据通过鼠标悬浮 tooltip 展示
3. tooltip 改为 `trigger: 'axis'`，悬浮时同时展示该基层单位的专项和配套数据

## 二、当前状态

文件：`e:\trae_demo_env\营收看板\src\views\TechResearchDashboard.vue`（`updateExpenseChart` 函数内）

| 行号 | 内容 | 作用 |
|------|------|------|
| 678–690 | `labelFormatter` 函数 | 生成柱顶 rich text 标签（完成率/实际值/偏差）|
| 692–698 | `richConfig` 对象 | 标签 rich text 样式配置 |
| 700–713 | `tooltipFormatter` 函数 | tooltip 内容（当前 `trigger: 'item'`，单条）|
| 716 | `tooltip: { trigger: 'item', formatter: tooltipFormatter }` | tooltip 配置 |
| 741 | `label: { show: true, ... }` | 专项柱标签 |
| 748 | `label: { show: true, ... }` | 配套柱标签 |

## 三、具体修改

### 修改 1：删除 labelFormatter 和 richConfig（第 678–698 行）

删除这两个不再使用的代码块，减少死代码。

### 修改 2：重写 tooltipFormatter 为 axis 模式（第 700–713 行）

`trigger: 'axis'` 时 `params` 是数组，需遍历展示专项和配套两组数据：

```js
const tooltipFormatter = (params) => {
  const idx = params[0].dataIndex
  let result = '<b>' + params[0].name + '</b>'
  params.forEach(p => {
    if (p.seriesName === '计划线') return
    const type = p.seriesName === '专项' ? 'special' : 'supporting'
    const item = list[idx][type]
    const plan = item.plan
    const actual = item.actual
    const rate = plan ? Math.round(actual / plan * 100) : 0
    const dev = actual - plan
    const devSign = dev >= 0 ? '+' : ''
    const rateColor = rate >= 100 ? '#10b981' : '#ef4444'
    const devColor = dev >= 0 ? '#10b981' : '#ef4444'
    result += '<br/>--- ' + p.seriesName + ' ---<br/>' +
      '计划值：' + plan + '万<br/>' +
      '实际值：' + actual + '万<br/>' +
      '完成率：<span style="color:' + rateColor + '">' + rate + '%</span><br/>' +
      '偏差：<span style="color:' + devColor + '">' + devSign + dev + '万</span>'
  })
  return result
}
```

### 修改 3：tooltip 配置改为 axis + shadow（第 716 行）

```js
tooltip: { trigger: 'axis', axisPointer: { type: 'shadow' }, formatter: tooltipFormatter },
```

### 修改 4：两个 bar series 的 label 改为 show: false（第 741、748 行）

```js
label: { show: false }
```

## 四、效果对比

| 修改前 | 修改后 |
|--------|--------|
| 柱顶显示完成率%/实际值万/偏差万（3行） | 柱顶无任何标签 |
| tooltip 仅显示单条柱数据 | tooltip 同时展示专项+配套两组数据 |
| 悬浮仅高亮单根柱 | 悬浮高亮整列（shadow 指针） |

## 五、验证

1. 浏览器打开 http://localhost:5173/ → 科研技术看板
2. 确认柱子上无任何数字标签
3. 悬浮某基层单位列，tooltip 同时显示专项和配套的计划值/实际值/完成率/偏差
4. 切换 Q1–Q4 确认正常
