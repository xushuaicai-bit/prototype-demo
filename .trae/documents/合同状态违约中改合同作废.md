# 合同状态"违约中"改为"合同作废"计划

## Summary

将合同状态字段中的"违约中"改为"合同作废"，排序权重调整为最大值（排最后）。

## 文件

`e:\trae_demo_env\营收看板\src\components\supply-chain\SupplierContractReport.vue`

## 改动点（共3处）

### 1. 选项数组 L441
```js
// 修改前
const allContractStatus = ['未签订', '正常履约', '履约结束', '违约中']
// 修改后
const allContractStatus = ['未签订', '正常履约', '履约结束', '合同作废']
```

### 2. 排序权重 L1273
```js
// 修改前
const statusOrder = { '正常履约': 5, '履约结束': 6, '违约中': 7 }
// 修改后（合同作废排最后，权重最大）
const statusOrder = { '正常履约': 5, '履约结束': 6, '合同作废': 7 }
```

### 3. 样式映射 L1288
```js
// 修改前
'违约中': 'text-red-500 font-medium'
// 修改后
'合同作废': 'text-red-500 font-medium'
```

## Verification

1. 筛选器中"违约中"选项变为"合同作废"
2. 表格中合同作废的行排序在最后
