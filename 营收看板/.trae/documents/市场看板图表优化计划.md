# 市场看板图表优化计划

## 需求分析
用户要求优化市场看板中的图表展示，实现以下功能：
1. 切换视图时横轴显示不同维度（区域/项目类型）
2. 合并相关图表，同时展示数量和金额

## 实现步骤

### 1. 修改标签页配置
**文件**: `src/views/MarketDashboard.vue`

**修改内容**:
- 更新 `projectTabs` 和 `amountTabs`，添加"按区域"和"按项目类型"选项
- 更新 `bidTabs`、`contractTabs` 等，添加"按区域"和"按项目类型"选项

**代码位置**: 第305-333行

```javascript
const projectTabs = [
  { key: 'company', label: '分公司' },
  { key: 'region', label: '按区域' },
  { key: 'type', label: '按项目类型' }
]

const amountTabs = [
  { key: 'company', label: '分公司' },
  { key: 'region', label: '按区域' },
  { key: 'type', label: '按项目类型' }
]
```

### 2. 添加数据配置
**文件**: `src/views/MarketDashboard.vue`

**新增数据**:
```javascript
const regionCategories = ['长三角区域总部', '中原区域总部', '大湾区区域总部', '上海', '境外区域', '其他城市区域']

const projectTypeCategories = ['水务', '水环境治理', '水利', '固废处理与处置', '土壤修复', '城市更新', '市政路桥房建', '其他']
```

**修改位置**: 第336行附近添加

### 3. 修改图表初始化函数
**文件**: `src/views/MarketDashboard.vue`

**修改 `initBarChart` 函数**:
- 支持动态切换横轴数据源
- 根据不同 tab 显示不同的 categories
- 确保 Y 轴自适应数据范围

**代码位置**: 第371-424行

### 4. 合并图表实现
**文件**: `src/views/MarketDashboard.vue`

#### 4.1 市场跟踪（合并项目数量和金额）
**目标**: 将"市场跟踪项目"和"市场跟踪金额"合并为一个图表
**位置**: 第119-141行区域

**实现方式**:
- 使用双 Y 轴柱状图
- 左 Y 轴显示项目数量
- 右 Y 轴显示金额
- 同一柱子同时显示数量和金额

#### 4.2 项目投标（合并数量和金额）
**目标**: 将"项目投标数量"和"项目投标金额"合并
**位置**: 第171-214行区域

**实现方式**: 
- 使用双 Y 轴柱状图
- 合并为一个图表组件

#### 4.3 总包合同（合并数量和金额）
**目标**: 将"总包合同数量"和"总包合同金额"合并
**位置**: 第217-293行区域

**实现方式**:
- 使用双 Y 轴柱状图
- 合并为一个图表组件

#### 4.4 项目立项管理（改为饼图）
**目标**: 使用饼图同时显示项目数量和金额
**位置**: 第143-168行区域

**实现方式**:
- 创建饼图，显示不同类型
- 每个扇区显示数量和金额
- tooltip 显示详细信息

### 5. 实现图表切换逻辑
**文件**: `src/views/MarketDashboard.vue`

**新增函数**:
```javascript
const getCategories = (tabKey) => {
  switch(tabKey) {
    case 'region': return regionCategories
    case 'type': return projectTypeCategories
    default: return companyCategories
  }
}

const getProjectChartData = (tabKey) => {
  // 根据 tabKey 返回对应的数据
  // 包含数量和金额两个系列
}

const getBidChartData = (tabKey) => {
  // 根据 tabKey 返回投标相关数据
}

const getContractChartData = (tabKey) => {
  // 根据 tabKey 返回合同相关数据
}
```

### 6. 修改 watch 监听器
**文件**: `src/views/MarketDashboard.vue`

**位置**: 第298行后

**新增逻辑**:
```javascript
watch([activeProjectTab, activeAmountTab], ([newProjectTab, newAmountTab]) => {
  // 重新渲染合并后的市场跟踪图表
  updateMarketTrackingChart()
})

watch([activeBidTab, activeBidAmountTab], () => {
  // 重新渲染合并后的投标图表
  updateBidChart()
})

watch([activeContractTab, activeContractAmountTab], () => {
  // 重新渲染合并后的合同图表
  updateContractChart()
})
```

## 具体修改清单

### 模板部分（Template）
1. **合并市场跟踪图表**（第99-141行）
   - 删除单独的"市场跟踪金额"卡片
   - 修改"市场跟踪项目"卡片，使用双 Y 轴图表

2. **合并投标图表**（第171-215行）
   - 删除单独的"项目投标金额"卡片
   - 修改"项目投标数量"卡片为合并版本

3. **合并合同图表**（第217-294行）
   - 删除单独的"总包合同金额"卡片
   - 修改"总包合同数量"卡片为合并版本

4. **饼图优化**（第143-168行）
   - 修改饼图显示，同时展示数量和金额

### 脚本部分（Script）
1. 新增区域和项目类型数据配置（第336行后）
2. 修改 `initCharts` 函数，实现动态切换
3. 新增数据获取函数
4. 修改 watch 监听器

## 注意事项
1. 保持现有 Mock 数据结构不变
2. 确保图表响应式布局
3. tooltip 清晰显示数量和金额
4. 图例清晰标识不同指标
5. Y 轴标签根据数据范围自适应
